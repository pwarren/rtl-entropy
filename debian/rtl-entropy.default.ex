# Defaults for rtl-entropy initscript
# sourced by /etc/init.d/rtl-entropy
# installed at /etc/default/rtl-entropy by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
