language: c
compiler:
  - gcc
  - clang
before_script:
  - sudo apt-get -qq update
  - sudo apt-get install -y libssl-dev libcap-dev librtlsdr-dev libbladerf-dev
  - mkdir build
  - cd build
  - cmake ..
script: make
